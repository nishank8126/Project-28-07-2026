// RE_PENDING (Piece 4A cleanup). Range3d / range algorithms (extend/contain/
// intersect/transform) are NOT in the verified M1-M6 slice. Range transformation
// especially requires reverse-engineered evidence. Re-add only with confirmed
// RE. See EvidenceLedger (E32 / RE_PENDING).
