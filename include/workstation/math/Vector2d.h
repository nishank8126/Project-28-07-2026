// RE_PENDING (Piece 4A cleanup). Vector2d / 2D types are NOT in the verified
// M1-M6 slice. Vector operations (add/sub/scale/dot/cross/normalize/length)
// are not authorized here and have been removed. Re-add only with confirmed
// reverse-engineering evidence. See EvidenceLedger (E31 / RE_PENDING).
