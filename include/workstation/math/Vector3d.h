// RE_PENDING (Piece 4A cleanup). Vector3d is NOT in the verified M1-M6 slice.
// Vector operations (add/sub/scale/dot/cross/normalize/length/IsZero) are not
// authorized here and have been removed. Re-add only with confirmed reverse
// -engineering evidence. See EvidenceLedger (E31 / RE_PENDING).
